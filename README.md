# Convolutional Neural Network Model with the Fashion MNiset
I used a convolutional neural network, which is neural network that detects images, to sort fashion data into specific categories. The training csv file is too big for the gitHUB repository. 
